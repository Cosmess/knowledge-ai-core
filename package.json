{
  "name": "knowledge-ai-core",
  "version": "0.1.0",
  "private": true,
  "description": "Backend, MCP server, RAG core and infrastructure for the Knowledge AI Platform.",
  "scripts": {
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @knowledge-ai/knowledge-api start:dev",
    "dev:mcp": "pnpm --filter @knowledge-ai/dev-knowledge-mcp start:dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "eslint": "^9.8.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.6.0"
}
